- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.xinitrc: .xinitrc
    ~/.gitconfig: .gitconfig
    ~/.vim: .vim
    ~/.vimrc: .vimrc

- shell: 
    - [git submodule update --init --recursive, Installing/Updating submodules]
