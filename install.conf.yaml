- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.xinitrc: .xinitrc
    ~/.gitconfig: .gitconfig
    ~/.vim: .vim
    ~/.vimrc: .vimrc
    ~/.Xdefault: .Xdefault
    ~/.xmonad: .xmonad

- shell: 
    - [git submodule update --init --recursive, Installing/Updating submodules]
